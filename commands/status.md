---
description: Update and circulate the weekly status deck
---

Use the `circle-status-deck` skill. Week commencing: $ARGUMENTS (default: next
Monday).

Copy last week's `Status_DDMMYYYY.pptx` forward, update the Show & Tell
divider (slide 3) and the housekeeping rota from the Housekeeping slide, and
leave the department slides alone unless the user says otherwise.

Then draft the circulation email in the usual shape (link to the deck, who is
on Show & Tell and Kitchen Duty), pulling the recipient list from last week's
sent copy rather than rebuilding it. Show the text before sending.
